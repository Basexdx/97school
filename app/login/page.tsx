'use client';
import Link from 'next/link';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
export default function Login(){const r=useRouter();const [error,setError]=useState('');function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const email=String(f.get('email')||'').trim();if(!email){setError('Введите email');return}localStorage.setItem('97user',JSON.stringify({name:email.split('@')[0],email,role:'student'}));r.push('/dashboard')}return <main className="auth"><Link className="logo" href="/">97<span>school</span></Link><form className="authCard" onSubmit={submit}><small>ДОБРО ПОЖАЛОВАТЬ</small><h1>Вход</h1><p>Продолжи обучение и сохраняй свой прогресс.</p><label>Email</label><input name="email" type="email" placeholder="student@school.ru"/><label>Пароль</label><input name="password" type="password" placeholder="••••••••"/><button className="primary">Войти</button>{error&&<p className="notice">{error}</p>}<p>Нет аккаунта? <Link href="/register">Зарегистрироваться</Link></p></form></main>}