'use client';
import Link from 'next/link';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
export default function Register(){const r=useRouter();const [role,setRole]=useState('student');function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const user={name:String(f.get('name')||'Ученик'),email:String(f.get('email')||''),grade:String(f.get('grade')||''),role};localStorage.setItem('97user',JSON.stringify(user));r.push('/dashboard')}return <main className="auth"><Link className="logo" href="/">97<span>school</span></Link><form className="authCard" onSubmit={submit}><small>НОВЫЙ АККАУНТ</small><h1>Регистрация</h1><div className="rolePick"><button type="button" className={role==='student'?'picked':''} onClick={()=>setRole('student')}>Ученик</button><button type="button" className={role==='teacher'?'picked':''} onClick={()=>setRole('teacher')}>Учитель</button></div><label>Имя</label><input required name="name" placeholder="Алексей"/><label>Email</label><input required name="email" type="email" placeholder="alex@school.ru"/>{role==='student'&&<><label>Класс</label><select name="grade" defaultValue="7"><option>5</option><option>6</option><option>7</option><option>8</option><option>9</option><option>10</option><option>11</option></select></>}<label>Пароль</label><input required name="password" type="password" minLength={6}/><button className="primary">Создать аккаунт</button><p>Уже зарегистрирован? <Link href="/login">Войти</Link></p></form></main>}