{
  "name": "genius-prototype-v14-grade7",
  "version": "1.4.0",
  "private": true,
  "scripts": {
    "prepare:grade7": "node scripts/prepare-grade7-bank.mjs",
    "predev": "npm run prepare:grade7 && npm run bank:build",
    "dev": "concurrently -k -n API,WEB -c auto \"npm:dev:api\" \"npm:dev:web\"",
    "dev:web": "next dev --hostname 127.0.0.1",
    "dev:api": "wrangler dev --config wrangler.jsonc --port 8787",
    "prebuild": "npm run prepare:grade7 && npm run bank:build",
    "build": "next build",
    "start": "next start",
    "db:local": "wrangler d1 execute DB --config wrangler.jsonc --local --file=cloudflare/schema.sql --yes",
    "setup:local": "node scripts/setup-local.mjs",
    "audit:clean": "npm audit --audit-level=high",
    "bank:build": "node scripts/build-task-bank.mjs",
    "db:tasks": "wrangler d1 execute DB --config wrangler.jsonc --local --file=cloudflare/task-bank.sql --yes",
    "db:tasks:v14": "wrangler d1 execute DB --config wrangler.jsonc --local --file=cloudflare/task-bank-v14-multigrade.sql --yes",
    "test:bank": "node --test tests/task-checker.test.mjs tests/integration.test.mjs && python3 tests/task-bank-sql.py",
    "start:web": "next start --hostname 127.0.0.1 --port 3000"
  },
  "dependencies": {
    "next": "16.3.5",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "fake-indexeddb": "^6.2.5",
    "wrangler": "^4.42.0",
    "esbuild": "0.28.1"
  }
}
